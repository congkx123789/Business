# proxy_human_like_automation

## 快速开始
- 创建并激活虚拟环境：`python -m venv venv`，然后在 PowerShell 中执行 `venv\Scripts\Activate.ps1`
- 安装依赖：`pip install -r requirements.txt`，随后执行 `playwright install`
- 根据实际帐号与代理配置编辑 `config.yml`
- 运行：`python main.py`

